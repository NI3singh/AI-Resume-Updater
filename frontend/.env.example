# Frontend calls the backend through the same-origin proxy (see next.config.js).
NEXT_PUBLIC_API_URL=/backend-api

# Where the Next dev server proxies /backend-api/* to (server-side only).
BACKEND_ORIGIN=http://127.0.0.1:8000
